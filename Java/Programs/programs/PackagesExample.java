package programs;

public class PackagesExample {

    /**
     * Registration logic => related classes => registration package
     * Payment Logic => related classes => payment package
     */

    /** ACCESS MODIFIERS
     * public => access by Any packages and classes in the project.
     * private => access by the declared package only.
     * protected => no access is provided to any other.
     * default => not declared => accessed by the same class.
     */
}
